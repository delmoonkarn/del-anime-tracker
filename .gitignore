# dependencies
node_modules/

# next.js
.next/
out/

# build output (Vite + esbuild)
dist/
dist-server/

# env / secrets
.env*
!.env.example

# editor / OS
.DS_Store
.vscode/
.idea/
Thumbs.db

# logs
*.log
npm-debug.log*

# typescript incremental cache
*.tsbuildinfo

# user data — stays local
data/

# build artifacts (only at root — icon/icon.ico is source, do keep it)
# ATracker.exe is committed intentionally as a prebuilt launcher.
/icon.ico
/AnimeTracker.exe
/_pkg_warmup.exe

# Claude Code local settings — machine-specific, don't commit
.claude/
